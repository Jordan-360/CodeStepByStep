strings : reverse

Write a method named reverse that accepts a string parameter returns a new string with the characters in the opposite order. 
For example, A call of reverse("Pikachu") would return "uchakiP".

  public static String reverse(String str) {
        String str2 = "";
        for(int i = str.length() - 1; i >= 0; i--) {
            str2 += str.charAt(i);
        }
        return str2;
    }



